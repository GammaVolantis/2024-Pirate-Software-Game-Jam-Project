using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class baker_test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        float test = Time.deltaTime;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
